<?xml version="1.0" encoding="UTF-8"?>
<Context>

    <!-- PowerAuth 2.0 Credential Service URL -->
    <Parameter name="powerauth.credentials.service.url" value="${POWERAUTH_DATA_ADAPTER_URL}"/>

    <!-- PowerAuth 2.0 Next Step Service URL -->
    <Parameter name="powerauth.nextstep.service.url" value="${POWERAUTH_NEXTSTEP_URL}"/>

    <!-- PowerAuth 2.0 Server Service URL -->
    <Parameter name="powerauth.service.url" value="${POWERAUTH_SERVICE_URL}"/>

    <!-- PowerAuth 2.0 Server Service Security Settings -->
    <Parameter name="powerauth.service.security.clientToken" value="${POWERAUTH_SERVICE_SECURITY_TOKEN}"/>
    <Parameter name="powerauth.service.security.clientSecret" value="${POWERAUTH_SERVICE_SECURITY_SECRET}"/>
    <Parameter name="powerauth.service.ssl.acceptInvalidSslCertificate" value="false"/>

    <!-- PowerAuth 2.0 Push Server URL -->
    <Parameter name="powerauth.push.service.url" value="${POWERAUTH_PUSH_SERVER_URL}"/>
    <Parameter name="powerauth.push.service.appId" value="1"/>

    <!-- PowerAuth 2.0 WebFlow Page Customization -->
    <Parameter name="powerauth.webflow.page.title" value="PowerAuth 2.0 Web Auth"/>
    <Parameter name="powerauth.webflow.page.ext-resources.location" value="file:/opt/ext-resources/"/>
    <Parameter name="powerauth.webflow.page.custom-css.url" value=""/>

    <!-- Database Configuration - JDBC -->
    <Parameter name="spring.datasource.url" value="${DB_WEBFLOW_URL}"/>
    <Parameter name="spring.datasource.username" value="${DB_WEBFLOW_USERNAME}"/>
    <Parameter name="spring.datasource.password" value="${DB_WEBFLOW_PASSWORD}"/>
    <Parameter name="spring.datasource.driver-class-name" value="${DB_WEBFLOW_DRIVER}"/>
    <Parameter name="spring.jpa.properties.hibernate.default_schema" value="${DB_WEBFLOW_SCHEME}"/>
    <Parameter name="spring.jpa.hibernate.ddl-auto" value="none"/>

    <!-- Database Configuration - JNDI -->
    <!--Parameter name="spring.datasource.jndi-name" value=""/-->

</Context>
