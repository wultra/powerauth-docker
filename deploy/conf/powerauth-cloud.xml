<?xml version="1.0" encoding="UTF-8"?>
<Context>

    <!-- PowerAuth Server Service Configuration -->
    <Parameter name="powerauth.service.url" value="${CLOUD_POWERAUTH_SERVICE_URL}"/>
    <Parameter name="powerauth.service.security.clientSecret" value="${CLOUD_POWERAUTH_SERVICE_SECURITY_CLIENT_SECRET}"/>
    <Parameter name="powerauth.service.security.clientSecret" value="${CLOUD_POWERAUTH_SERVICE_SECURITY_CLIENT_TOKEN}"/>

    <!-- PowerAuth Push Service configuration -->
    <Parameter name="powerauth.push.service.url" value="${CLOUD_PUSH_SERVER_URL}"/>

</Context>
